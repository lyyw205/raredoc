---
name: card-merge
description: /test 등에서 "같은 카드인데 따로 떨어진 LogicalCard 들"을 발견했을 때, 정말 같은 그림(일러)인지 이미지로 확정한 뒤 하나의 Card 로 안전 병합하는 스킬. 사용자가 "이 둘(셋) 묶어줘", "같은 카드니까 병합해줘", "이거 합쳐줘", "merge these cards", "<setId>#<num> <setId>#<num> 묶기", "/test에서 본 이거 병합" 같이 카드 LC 병합을 요청하면 이 스킬을 쓴다. 핵심: 작가/gameCard 일치는 후보 필터일 뿐 확정이 아니고(언노운 함정), **이미지를 직접 보고 같은 그림인지**가 유일한 확정 근거다. 비가역 작업이라 dry-run·동결가드·전건 확인을 글자 그대로 따른다.
---

# card-merge — 같은 그림 LC 안전 병합

DB 모델은 **`Card`(LogicalCard) = "한 그림"** 이고, 같은 그림의 지역·재판 인쇄본은 그 Card 아래 `RegionCard` 로 매달린다. 그런데 자동 그룹화(pHash)가 **프레임/홀로가 다른 재판**이나 **다른 팩에 발매된 같은 카드(교차지역 누락)** 를 못 묶어, 같은 그림이 여러 Card 로 쪼개진 경우가 있다. 이 스킬은 그걸 **사람이 이미지로 확정**한 뒤 하나로 합친다.

> **존재 이유**: 병합은 **비가역**(소스 LC 삭제)이다. 잘못 합치면 *다른 카드 두 장을 한 정체성으로* 무너뜨린다. 그래서 "작가/숫자가 같다" 정도로 자동 판정하지 않고, **이미지 직접 확인 → dry-run → (동결이면) 확인 → apply** 를 고정한다. 절차를 "효율화"하지 말 것 — 바꿀 필요가 보이면 사용자에게 먼저 말한다.

## ✅ 무엇을 확인하면 되나 (필수 체크리스트)

병합해도 되는 유일한 조건 = **정말 같은 카드(같은 그림)** 인가. 순서:

1. **★ 같은 그림(이미지)인가 — 확정 근거.** 후보 LC 들의 카드 이미지를 **직접 눈으로** 비교한다. 일러스트(그림)가 같으면 OK(프레임·홀로·언어텍스트가 달라도 무방). **이것만이 확정이다.**
2. **같은 카드인가 — 보조 확인.** 같은 포켓몬/이름이어야 한다. `gameCard` 가 동일하면(이름+효과 동일) 강한 신호. **gameCard 가 다르면** 정말 같은 카드인지 한 번 더 의심한다(스탯 차이 = 다른 카드일 수 있음).
3. **동결팩이면 `--allow-protected` 자동(사전승인됨).** 병합은 EN/KR 매칭을 바꾸지만, **card-merge 작업 한정으로 사용자가 사전 승인**(2026-06-23)했고 **이미지 확정(§1)이 진짜 안전장치**다 → 동결팩이어도 매번 따로 안 묻고 `--allow-protected` 로 진행. (★`PROTECTED_GROUPS` 목록 자체는 유지 — merge-en-identity·build-group 등 다른 스크립트엔 그대로 적용.)

작가(illustrator)·gameCard 일치는 **후보를 좁히는 필터**일 뿐이다. **둘 다 같아도 다른 그림일 수 있다**(예: 언노운 A~Z 는 같은 작가·같은 gameCard·다른 글자그림 → 병합 금지). 그래서 1번(이미지)이 빠지면 안 된다.

## 🚫 절대 금지 (HARD STOP)

1. **이미지를 안 보고 병합하지 않는다.** 작가·gameCard·번호만 보고 자동 병합 금지(언노운 함정). 반드시 §1 inspect 의 🖼 이미지를 Read 로 보고 같은 그림임을 확정한다.
2. **불확실하면 병합하지 않는다.** 이미지가 애매(저화질·크롭 다름)하면 합치지 말고 사용자에게 판단을 구한다. 원칙: **미병합 > 오병합**(비가역).
3. **dry-run 먼저.** `merge-logical-cards.ts` 는 항상 dry-run 으로 계획을 보여준 뒤 `--apply`.
4. **동결팩은 `--allow-protected` 자동(사전승인)이지만, 이미지 확정 없이는 여전히 금지.** card-merge 한정 동결 사전승인(2026-06-23)이라 동결팩이어도 따로 안 묻고 진행한다 — 단 그 게이트는 §1 **이미지 확정**이다(이미지 안 보고 진행하면 #1 위반). 즉석 prisma/SQL 우회 금지(`merge-logical-cards.ts` 경유).
5. **타깃은 정보가 가장 풍부한 LC.** locale 수가 많은 LC(특히 JP 앵커 보유)를 타깃(생존)으로, 나머지를 소스로 합친다. 임의로 EN-only LC 를 타깃 삼지 않는다.

## 파이프라인 (순서 불변)

### 0. 후보 LC 확보
사용자가 `/test` 에서 "이거 같은 카드 같다" 며 지목한 카드들. 입력은 화면에 보이는 **`<setId>#<number>` 로케이터**(예: `jp-tcg-SMP2#2`)나 **LC id**(`lc-...`) — 혼용·여러 개 가능. (`/test` 의 점선 **⊕ 병합후보** 박스는 *느슨한* 후보이니 그대로 믿지 말고 §1 로 검증한다.)

### 1. INSPECT — 같은 그림인지 확정 (읽기 전용)
```bash
npx tsx .claude/skills/card-merge/scripts/inspect-merge.ts <loc1> <loc2> [<loc3> ...]
```
출력: 각 LC 의 이름·supertype·HP·작가·gameCard·지역·팩(🔒=동결) + 교차점검(gameCard/작가 동일·동결팩) + 후보 이미지를 작게 줄여 **1장 가로 몽타주** `/tmp/merge-inspect/_montage.png` 로 합쳐 저장(왼→오 순서도 출력).
- **★토큰 절감: `_montage.png` 1장만 Read 한다(개별 카드 N장 Read 금지).** 왼→오 순서대로 같은 일러인지 본다. 스크립트 텍스트 출력에 필드·동결·gameCard 다 있으니 **별도 DB 쿼리는 최소화**(inspect 가 이미 다 줌).
- gameCard 가 다르면 "정말 같은 카드인지" 추가 확인(텍스트 출력의 이름·HP + 몽타주).
- 판정: **같은 그림 확정 → §2** / **다른 그림(언노운류) → 병합 안 함, 사용자에게 "이건 다른 그림이라 따로 둠" 보고** / **애매 → 사용자에게 판단 요청.**

### 2. 타깃 선정
생존할 타깃 LC = **locale 수가 가장 많은 것**(동률이면 **JP locale 보유** = `lc-orphan-jp-*` 우선 / 그래도 동률이면 id 안정순). 나머지 LC = 소스. (타깃 = "한 그림"의 대표가 된다.)

### 3. dry-run → apply (동결팩 사전승인 — 매번 안 물음)
소스마다 한 번씩 타깃으로 합친다(타깃 고정). **§1 이미지 확정만 됐으면 동결팩이어도 바로 진행.** `--allow-protected` 는 항상 붙인다(비동결엔 무영향이라 분기 불필요):
```bash
# 1) dry-run 으로 계획·의존 이전·동결여부 확인:
npx tsx scripts/merge-logical-cards.ts <srcLC> <tgtLC> --allow-protected
# 2) 이미지 같은 그림 확정됐으면 적용:
npx tsx scripts/merge-logical-cards.ts <srcLC> <tgtLC> --apply --allow-protected
```
- `merge-logical-cards.ts` 가 소스의 RegionCard·CardText(언어충돌은 타깃권위로 삭제)·CardSpecies(중복 cascade)·ExternalIdMapping(충돌회피)·CollectionItem·Trade·Ruling·DeckRecipeCard 를 모두 타깃으로 이전하고 소스 LC 를 삭제한다. 트랜잭션·동결가드 내장.
- 3개 이상이면 소스를 하나씩 같은 타깃으로 반복 적용한다.

### 4. 검증 (토큰 절감 — 스크립트 출력 우선)
`merge-logical-cards.ts` 가 적용 직후 `✅ 병합 완료. 타깃 <tgt> locale: [JP,EN,KR]` 를 출력한다(트랜잭션 후 실제 재조회). **그 출력으로 1차 확인** — 의도한 지역이 다 들어왔으면 OK. 매 건 별도 쿼리하지 말 것.
- 배치(여러 쌍)가 끝났을 때 **한 번만** 종합 확인(원하면): `SELECT count(*) FROM "LogicalCard" WHERE id IN ('<src...>')` = 0.

### 5. 보고
사용자에게 한국어로: 병합한 그룹(어느 LC 들 → 어느 타깃, **이미지로 같은 그림 확인** 근거 한 줄), 동결 override 했으면 그 사실, 검증 결과(타깃 locale·소스 삭제). 다른 그림이라 안 합친 후보도 사유와 함께. 코드·세트명 원문 유지.

## 유의사항 (반복 함정)

- **작가·gameCard 같아도 다른 그림 가능** — 언노운 글자, 다른시대 같은 트레이너 프로모(Champions Festival 등), 같은 작가의 알트아트. 이미지가 최종 판정.
- **pHash 거리로 자동 판정 불가** — "같은 그림 다른 프레임"(22~42비트)과 "다른 그림 같은 레이아웃"(언노운 19~42비트)이 겹친다. 그래서 사람 눈이다.
- **이미지 내용≠파일명/CDN** 함정 — 가끔 저장된 이미지가 잘못 들어가 있다(다른 지역 카드가 박힘). inspect 이미지가 카드와 안 맞으면 그 자체가 별개 버그 — 병합 판정에 쓰지 말고 따로 보고.
- **N장 병합** — 타깃 1개 고정, 소스들을 하나씩. 한 번에 한 쌍씩 dry-run/apply 가 안전.
- 스크립트는 **레포 루트에서** 실행(dotenv 가 루트 `.env` 로드).

## 자산
- `scripts/inspect-merge.ts` — 검증 엔진(필드+이미지 다운로드+교차점검). **읽기 전용.** 입력: LC id 또는 `<setId>#<num>`.
- `scripts/merge-logical-cards.ts`(레포 루트 `scripts/`) — 병합 엔진. **DB 변경 유일 경로**, 트랜잭션·동결가드(`--allow-protected`)·dry-run 내장.
- 재사용: `scripts/lib/protected-groups.ts`(동결 단일출처), `/test`(시각 검토·⊕ 병합후보 박스=느슨한 힌트).
